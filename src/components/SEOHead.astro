---
import type { CollectionEntry } from 'astro:content';
import SEOHeadContent from '../components/SEOHeadContent.astro';

const { title, description, heroImage } = Astro.props;
---
<SEOHeadContent title={title} description={description} image={heroImage} />
